with open('just_the_first.txt') as first_line_doc:
    # Step 2: Save the first line of just_the_first.txt into the variable first_line
    first_line = first_line_doc.readline()

    # Step 3: Print out the variable first_line
    print(first_line)
